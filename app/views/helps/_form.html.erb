<%= form_with(model: help, local: true) do |form| %>
  <% if help.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(help.errors.count, "error") %> prohibited this help from being saved:</h2>

      <ul>
        <% help.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :contact, 'Contact Information' %>
    <%= form.text_field :contact, class: 'form-control'%>
  </div>
  <br />

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, class: 'form-control'%>
  </div>
  <br/>
  <div class="actions">
    <%= form.submit 'Submit', class: 'btn btn-primary'%>
  </div>
<% end %>
